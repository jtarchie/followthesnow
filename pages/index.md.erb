This is an aggregate list of ski resorts and the expected snow totals for the day.
Follow the snow started after discussion of "Where we can find the best snow today?"

## Locations
      
<%- by_state.keys.each do |state| -%>
* [<%= state %>](#<%= state.downcase.gsub(/\s+/,'-') %>)
<%- end -%>

*Last Updated:* <%= current_timestamp %>

<%- by_state([Forecast::Aggregate, Forecast::Emoji]).each do |state, predictions| -%>
## <%= state %>
<%- number_of_days = predictions.first.days.length -%>

| Location | <%= predictions.first.days.join(' | ') %> |
| <%= (['-'] * (number_of_days + 1)).join(' | ') %> |            
<%- predictions.each do |prediction| -%>
| [<%= prediction.name %>](<%= prediction.url%>) | <%= prediction.ranges[0...number_of_days].join(' | ') %> |
<%- end -%>

<%- end -%>