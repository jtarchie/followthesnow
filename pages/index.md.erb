
We provide daily predictions for resorts across the US and Canada, sorted by
location, state/province. Every day early morning, predictions for snow totals
are updated for the week.

<% countries.each do |country| %>

## <%= h country %> {#<%= country.parameterize %>}
<% states(country: country).each do |state| %>
* [<%= h state %>](/states/<%= state.parameterize %>)
<% end %>

<% end %>

*Last Updated:* <%= h current_timestamp %>